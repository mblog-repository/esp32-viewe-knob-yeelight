# Каталоги сборки Arduino CLI (создаются в <sketch>/build)
*/build/
build/

# PlatformIO (если кто-то соберёт через него)
.pio/

# Локальные настройки редакторов / Claude Code
.claude/
.vscode/
.DS_Store

# Свопы/бэкапы редакторов
*.swp
*.swo
*~

# СЕКРЕТЫ: Wi-Fi пароли и т.п. — НИКОГДА не коммитим.
# В репозиторий идёт только шаблон secrets.h.example
secrets.h
*/secrets.h
**/secrets.h
